<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Captain's Log Show</title>
</head>
<body>
    <h1>Captain's Logs show page</h1>
    <p>
        The <%= logs.title %> is <%= logs.entry %>.
        <% if (logs.shipIsBroken === true) { %>
        <span>This ship is broken. </span>
    <% } else { %>
    <span>This ship is not broken.</span>
    <% } %>
    <a href = "/logs">Back to Index</a>
    </p>
</body>
</html>