<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index page for Captain's Log</title>
</head>
<body>
    <h1>Captain Index</h1>
    <nav>
        <a href="/logs/new">Create a new Captain's Log</a>
    </nav>
    <ul>
        <% for(let i=0; i<Data.length; i++) {%>
            <li>
                Here is <%=Data[i].title; %> and <%=Data[i].entry; %>
            <% if (Data[i].shipIsBroken === true) { %>
                The Ship is broken
            <% } else { %>
                The Ship is not broken
            <% } %>
            </li>
    <% } %>
    </p>
    
</body>
</html>